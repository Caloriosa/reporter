/**
 * Caloriosa reporter (world) theme
 * Author: PurrplingCat
 */

body {
  font-family: $body-font-family
}

h1, h2, h3, h4, h5, h6 {
  font-family: $heading-font-family !important;
}

.error-msg {
  color: #ffffff;
}

.full-width: {
  margin: 0;
  width: 100% !important;
}
